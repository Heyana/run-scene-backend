# 1. 上传到 NAS

scp 3d-editor-backend-linux.tar.gz admin@nas-ip:/volume1/docker/

# 2. SSH 连接

ssh admin@nas-ip

# 3. 解压

cd /volume1/docker/3d-editor-backend
tar -xzf 3d-editor-backend-linux.tar.gz

# 4. 安装

chmod +x install.sh
./install.sh

# 5. 配置

nano config.yaml

# 6. 启动

sudo systemctl start 3d-editor-backend

nas_enabled: true
nas_path: /vol1/1003/project/editor_v2/static/textures

打包

wsl bash -c "cd /mnt/e/hxy/my-project/run-scene-backend && /usr/local/go/bin/go build -o app-linux-fixed main.go"

# SSH 到 NAS

ssh LiLeng@192.168.3.10

# 进入目录

cd /vol1/1003/project/editor_v2/deploy

# 停止旧服务

pkill -f app-linux

# 备份旧文件

mv app-linux app-linux.old

# 使用新文件

mv app-linux-fixed app-linux

# 赋予执行权限

chmod +x app-linux

# 启动

nohup ./app-linux > logs/app.log 2>&1 &

# 查看日志

tail -f logs/app.log

# 安装

sudo dpkg -i Clash.Verge_2.4.4_amd64.deb

# 如果提示缺少依赖，修复依赖

sudo apt install -f

# 再次安装（如果上一步修复了依赖）

sudo dpkg -i Clash.Verge_2.4.4_amd64.deb

# 验证安装

which clash-verge

# 查看版本

clash-verge --version

cd /vol1/1003/app

# 下载

wget https://github.com/MetaCubeX/mihomo/releases/download/v1.19.19/mihomo-linux-amd64-v1.19.19.gz

# 解压

gunzip mihomo-linux-amd64-v1.19.19.gz

# 重命名

mv mihomo-linux-amd64-v1.19.19 mihomo

# 赋予执行权限

chmod +x mihomo

# 创建配置目录

mkdir -p ~/.config/mihomo

# 下载你的订阅配置（替换成你的订阅链接）

wget -O ~/.config/mihomo/config.yaml "https://103.200.216.101/proxy?url=https%3A%2F%2Fxn--wtq35pfyd55o.co%2Fanswer%2Fland%2F8aa60a5a582b6488dbc0d02a18888b30"

# 或者手动创建配置文件

nano ~/.config/mihomo/config.yaml

HTTP proxy listening at: [::]:7890
SOCKS proxy listening at: [::]:7891
RESTful API listening at: 127.0.0.1:9090

# 测试代理

curl -x http://127.0.0.1:7890 https://www.google.com

# 进入后端目录

cd /vol1/1003/project/editor_v2/deploy

# 编辑配置

nano config.yaml
