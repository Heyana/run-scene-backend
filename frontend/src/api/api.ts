import * as texture from "./models/texture";
import * as tag from "./models/tag";
import * as backup from "./models/backup";
import * as security from "./models/security";
import * as system from "./models/system";

export const api = {
  texture,
  tag,
  backup,
  security,
  system,
};
